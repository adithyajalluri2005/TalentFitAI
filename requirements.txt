# --- Web layer ---
fastapi
uvicorn[standard]
python-multipart          # required by FastAPI for UploadFile / Form
pydantic
sqlalchemy
python-dotenv

# --- LLM / agent stack ---
langchain
langgraph
langchain_community
langchain_core
langchain_groq
groq                      # used directly for Whisper transcription
tavily-python

# --- NLP / matching ---
# No sentence-transformers/torch on purpose: embeddings come from the HF
# Inference API (see nodes.py get_embedding), which keeps this under a 512MB tier.
scikit-learn
nltk
numpy
pypdf
httpx
